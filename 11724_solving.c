#include <stdio.h>

typedef struct node
{
    int number;
    struct node* next;
} node;

node graph[1001];

int main(void)
{
    int N = 0;
    int M = 0;
    scanf("%d %d", &N, &M);
    for (int i = 0; i < M; i++)
    {
        
    }
}