#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct node
{
    int number;
    struct node* next;
} node;

typedef struct stack
{
    
} stack;

int main()
{
    int N = 0;
    int M = 0;
    int R = 0;
    scanf("%d %d %d", &N, &M, &R);
    for (int i = 0; i < M; i++)
    {
        
    }
    return 0;
}