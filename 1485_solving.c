#include <stdio.h>

int main(void)
{
    int N = 0;
    int x[4];
    int y[4];
    int dist[6];
    scanf("%d", &N);
    for (int i = 0; i < N; i++)
    {
        for (int j = 0; j < 4; j++)
        {
            scanf("%d %d", &x[j], &y[j]);
        }
        if (x)
    }
}